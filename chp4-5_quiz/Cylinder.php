<?php
class Cylinder {
    // Create variables needed to calculate a Cylinder's area
    //*hint* the variables are used to reference objects

    function __construct($radius, $height){}

    /**
     * Get area of a cylinder
     * Equation = (2PI * radius * height) + (2PI * radius to the power of 2)
     */
    //Edit might need to be done in Rectangle class
    function getArea(){}
}